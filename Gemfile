source "https://rubygems.org"

# Keep the existing GitHub Pages build and generated feed/sitemap.
gem "github-pages", group: :jekyll_plugins
gem "wdm", "~> 0.1.0" if Gem.win_platform?

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end
